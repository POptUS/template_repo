# Tool Overview

Should we write something here that explains the high-level design, explains the
design philosophy, details certain design decisions, and demonstrates the
high-level interface of the code?

We can use both packages in the same application.
```
from mytemplate2 import compute
from mytemplate2.subC import compute_c

x = 1.1
print(f"y = {compute(x)}")
print(f"y_c = {compute_c(x)}")
```
